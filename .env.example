# Flask Application Settings
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here-change-this-in-production

# Flask environment: development | production
FLASK_ENV=development

# Flask debug mode: 0 = production (recommended), 1 = development only
FLASK_DEBUG=0

# Upload Configuration
UPLOAD_FOLDER=./print_order_web/static/uploads

# ConsumableClient API Configuration
# Set to "true" to enable real blockchain integration (requires Windows + DLL)
# Set to "false" to use stub mode for development/testing
ENABLE_API_MODE=false

# Path to ConsumableClient.dll v2.0.0.2
# Default: ../CCAPIv2.0.0.2/ConsumableClient.dll (relative to print_order_web directory)
# For production: Use absolute path or update relative path as needed
CONSUMABLE_DLL_PATH=../CCAPIv2.0.0.2/ConsumableClient.dll

# Anthropic API Key (Optional - for future AI features)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-api-key-here
