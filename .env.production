# PrintOrderWeb Production Configuration
# This file is bundled with the executable
#
# ARCHITECTURE:
# - NO STUB MODE - DLL is required for operation
# - Inventory refreshes every 30 seconds in background thread
# - Job submission uses dedicated threads with complete isolation

# Flask Settings
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=production-secret-key-change-this
FLASK_ENV=production
FLASK_DEBUG=0

# Upload folder (relative to executable)
UPLOAD_FOLDER=_internal/static/uploads

# DLL path (relative to executable - DLL is in _internal folder)
CONSUMABLE_DLL_PATH=_internal/ConsumableClient.dll

# =============================================================================
# Estimator Configuration (for demos/testing)
# =============================================================================
# These values control how toner usage is estimated for print jobs.
# Increase these values to consume more ink during demonstrations.
#
# Formula: toner_ml = sheets × (coverage/100) × base_ml × quality_modifier
#
# Demo presets:
#   Default:    ESTIMATOR_BASE_TONER_ML=0.15, ESTIMATOR_PAGE_COVERAGE_PERCENT=10
#   Demo 10x:   ESTIMATOR_BASE_TONER_ML=0.50, ESTIMATOR_PAGE_COVERAGE_PERCENT=100
#   Demo 33x:   ESTIMATOR_BASE_TONER_ML=1.0,  ESTIMATOR_PAGE_COVERAGE_PERCENT=100

# Base toner mL per sheet at 100% coverage (default: 0.15)
ESTIMATOR_BASE_TONER_ML=0.15

# Page coverage percentage (default: 10)
ESTIMATOR_PAGE_COVERAGE_PERCENT=10
